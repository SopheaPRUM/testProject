#include <string>
#include <algorithm>
#include <iterator>
#include <vector>
using namespace std;

class Tools
{
    public:
    	static std::vector<string> stringToStringVector(const std::string& str, char delim = ' ');
    	
};
 